#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL),cout.tie(NULL);

    string a,b;
    cin>>a>>b;
    lower

    if(a==b)
    {
        cout<<"0";
    }
   if(a>b)
   {
       cout<<"1";
   }
   if(a<b)
   {
       cout<<"-1";
   }
}
